import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'dps-926-app-4',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
